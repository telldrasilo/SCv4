# SwordCraft Idle Forge

Next.js app (`swordcraft-skin`): game shell UI, theme, and experiments.

## Development

```bash
npm install
npm run dev
```

The app listens on port **3000** by default. Run tests with `npm test`.

## Documentation

- **Weapon inventory module (design and integration):** see the documentation pack starting at [`docs/module/weapon-inventory/00-index.md`](docs/module/weapon-inventory/00-index.md) (scope, domain model, public API, integration contracts, i18n, data authority, risks, delivery manifest).

For day-to-day agent and task notes, see [`worklog.md`](worklog.md).
